{
  "name": "metoffice-forecast-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "A Met Office Site Specific forecast in the layout of the retiring Met Office display.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test \"test/**/*.test.mjs\"",
    "fixtures": "node tools/make-fixtures.mjs"
  }
}
